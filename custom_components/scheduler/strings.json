{
  "config": {
    "step": {
      "user": {
        "title": "Add scheduler",
        "description": "Create a new scheduler with its own calendar",
        "data": {
          "name": "Name"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manage scheduler",
        "description": "Current schedules:\n\n{schedules}",
        "menu_options": {
          "add": "Add schedule",
          "edit": "Edit schedule",
          "remove": "Remove schedule",
          "configure": "Default configuration"
        }
      },
      "add_schedule": {
        "title": "Add schedule",
        "description": "Create a new all-day schedule",
        "data": {
          "name": "Schedule name",
          "start_date": "Start date",
          "end_date": "End date"
        }
      },
      "select_schedule_to_edit": {
        "title": "Select schedule to edit",
        "description": "Choose a schedule to edit",
        "data": {
          "schedule": "Schedule"
        }
      },
      "select_schedule_to_remove": {
        "title": "Select schedule to remove",
        "description": "Choose a schedule to remove",
        "data": {
          "schedule": "Schedule"
        }
      },
      "edit_schedule": {
        "title": "Edit schedule",
        "description": "Update this schedule",
        "data": {
          "name": "Schedule name",
          "start_date": "Start date",
          "end_date": "End date"
        }
      },
      "remove_schedule": {
        "title": "Remove schedule",
        "description": "Are you sure you want to remove '{schedule_name}' ({start_date} to {end_date})?",
        "data": {
          "confirm": "Confirm removal"
        }
      },
      "configure": {
        "title": "Default configuration",
        "description": "Provide a YAML structure for default configuration. This will be stored in the 'default_configuration' attribute.",
        "data": {
          "configuration": "Default configuration (YAML)"
        }
      }
    },
    "error": {
      "overlap": "This schedule overlaps with an existing schedule",
      "end_before_start": "End date must be after start date",
      "invalid_date": "Invalid date format",
      "invalid_yaml": "Invalid YAML format",
      "not_dict": "Configuration must be a YAML structure, not a simple string"
    }
  }
}
